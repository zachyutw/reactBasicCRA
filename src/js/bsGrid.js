export default {
    bsCols:(cols) => ("col-xs-" + cols + " cols-sm-" + cols + " col-md-" + cols + " col-lg-" + cols),
    bsOffsets:(cols) => ("col-xs-offset-" + cols + " col-sm-offset-" + cols + " col-md-offset-" + cols + " col-lg-offset-" + cols),
}

export const bsCols=(cols) => ("col-xs-" + cols + " cols-sm-" + cols + " col-md-" + cols + " col-lg-" + cols);